# .claude Adapter

This folder contains command adapter files for .claude.

The real skill rules live in `.agents/skills/`.

Command files in this folder should only route the agent to the corresponding skill file and should not duplicate the full skill content.
